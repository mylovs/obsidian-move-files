# Move File 插件开发文档

## 1. 项目概述

### 1.1 项目简介

`Move File` 是一款 Obsidian 插件，旨在帮助用户更高效地管理和组织笔记中引用的外部文件。通过分析当前打开的 markdown 文件，自动识别所有引用的附件（如图片、PDF、视频等），并提供按扩展名分类选择、一键移动的功能。

### 1.2 功能特性

| 功能     | 描述                         |
| :----- | :------------------------- |
| 引用文件分析 | 自动识别 markdown 文件中引用的所有外部文件 |
| 扩展名分类  | 按文件扩展名分组展示，支持批量选择          |
| 移动源文件  | 支持将当前打开的 markdown 文件一起移动      |
| 智能目标路径 | 默认目标文件夹名为当前文件名（不含扩展名）      |
| 冲突自动处理 | 目标存在同名文件时自动跳过，避免覆盖        |
| 移动历史记录 | 保存所有移动操作记录，支持按时间戳分组查看    |
| 批量撤销操作 | 支持按时间戳撤销同一批次移动的所有文件      |

### 1.3 技术栈

- **框架**: Obsidian API
- **语言**: TypeScript
- **样式**: CSS3
- **存储**: localStorage（移动历史）

***

## 2. 项目结构

```
obsidian-move-file/
├── main.ts          # 插件核心逻辑（主入口）
├── manifest.json    # 插件配置文件
├── styles.css       # 插件样式文件
├── package.json     # Node.js 依赖配置
├── tsconfig.json    # TypeScript 配置
├── 需求文档.md      # 需求说明文档
└── 开发文档.md      # 开发说明文档
```

### 2.1 文件职责

| 文件              | 职责                | 状态 |
| :-------------- | :---------------- | :- |
| `main.ts`       | 插件核心逻辑、UI 渲染、文件操作 | 核心 |
| `manifest.json` | 插件元数据（ID、版本、描述等）  | 配置 |
| `styles.css`    | 模态框界面样式           | 样式 |
| `package.json`  | 依赖管理、脚本命令         | 配置 |
| `tsconfig.json` | TypeScript 编译配置   | 配置 |

***

## 3. 核心类与接口

### 3.1 数据接口

#### MoveFileRecord

```typescript
interface MoveFileRecord {
  sourcePath: string;  // 源文件路径
  destPath: string;    // 目标文件路径
  timestamp: number;   // 移动时间戳（同一批次共享）
  operation: 'move' | 'undo'; // 操作类型
}
```

#### ReferencedFile

```typescript
interface ReferencedFile {
  name: string;        // 文件名
  path: string;        // 文件完整路径
  extension: string;   // 文件扩展名（小写）
  exists: boolean;     // 文件是否存在
  selected: boolean;   // 是否选中
}
```

#### ExtensionGroup

```typescript
interface ExtensionGroup {
  extension: string;       // 扩展名（如: png, jpg, pdf）
  files: ReferencedFile[]; // 该扩展名的所有文件
  selected: boolean;       // 是否选中该组
}
```

#### HistoryGroup

```typescript
interface HistoryGroup {
  timestamp: number;       // 时间戳
  records: MoveFileRecord[]; // 同一时间戳的所有移动记录
}
```

### 3.2 核心类

#### MoveFileModal

**职责**: 提供可视化界面让用户选择和移动引用文件

**核心属性**:

| 属性名           | 类型               | 说明               |
| :------------- | :----------------- | :--------------- |
| `sourceFile`   | `TFile`           | 当前打开的源文件        |
| `groups`       | `ExtensionGroup[]` | 按扩展名分组的引用文件列表 |
| `destinationPath` | `string`       | 目标文件夹路径         |
| `moveSourceFile` | `boolean`      | 是否移动源文件         |

**核心方法**:

| 方法名                      | 功能               | 参数                                   | 返回值             |
| :----------------------- | :--------------- | :----------------------------------- | :-------------- |
| `constructor`            | 初始化模态框           | `app: App`, `file: TFile`            | -               |
| `onOpen`                 | 模态框打开时执行         | -                                    | `Promise<void>` |
| `onClose`                | 模态框关闭时执行         | -                                    | -               |
| `analyzeReferencedFiles` | 分析当前文件中的引用       | -                                    | `Promise<void>` |
| `moveFiles`              | 执行文件移动操作         | -                                    | `Promise<void>` |
| `render`                 | 渲染模态框界面          | -                                    | -               |

#### UndoMoveModal

**职责**: 提供撤销移动历史的可视化界面

**核心属性**:

| 属性名           | 类型                   | 说明               |
| :------------- | :--------------------- | :--------------- |
| `history`      | `MoveFileRecord[]`     | 移动历史记录列表       |

**核心方法**:

| 方法名                      | 功能               | 参数                                   | 返回值             |
| :----------------------- | :--------------- | :----------------------------------- | :-------------- |
| `constructor`            | 初始化模态框           | `app: App`                           | -               |
| `onOpen`                 | 模态框打开时执行         | -                                    | -               |
| `onClose`                | 模态框关闭时执行         | -                                    | -               |
| `undoMoveByTimestamp`    | 按时间戳批量撤销         | `timestamp: number`                  | `Promise<void>` |
| `clearHistory`           | 清空历史记录            | -                                    | -               |
| `render`                 | 渲染模态框界面          | -                                    | -               |

#### MoveFilePlugin

**职责**: 插件主类，负责注册命令和图标

**核心方法**:

| 方法名                 | 功能               | 参数 | 返回值             |
| :------------------ | :--------------- | :- | :-------------- |
| `onload`            | 插件加载时注册命令和图标 | -  | `Promise<void>` |
| `onunload`          | 插件卸载时清理资源    | -  | -               |

### 3.3 存储工具对象

#### Storage

**职责**: 统一管理移动历史的存储操作

**方法**:

| 方法名              | 功能               | 参数                                   | 返回值             |
| :----------------- | :--------------- | :----------------------------------- | :-------------- |
| `getHistory`       | 获取所有历史记录       | -                                    | `MoveFileRecord[]` |
| `saveHistory`      | 保存历史记录         | `records: MoveFileRecord[]`          | `void`          |
| `addRecord`        | 添加单条记录         | `record: MoveFileRecord`             | `void`          |
| `removeByIndex`    | 按索引删除记录        | `index: number`                      | `void`          |
| `removeByTimestamp`| 按时间戳批量删除      | `timestamp: number`                  | `void`          |

***

## 4. 核心流程

### 4.1 插件启动流程

```
用户打开 Obsidian
        ↓
插件加载 (onload)
        ↓
注册命令面板命令 (Move Referenced Files)
        ↓
注册命令面板命令 (Move History)
        ↓
注册侧边栏 Ribbon 图标
        ↓
等待用户触发
```

### 4.2 文件移动流程

```
用户触发命令
        ↓
打开 MoveFileModal
        ↓
分析当前文件引用 (analyzeReferencedFiles)
        ↓
使用 MetadataCache.resolvedLinks 获取链接
        ↓
使用 MetadataCache.getFileCache 获取嵌入文件
        ↓
按扩展名分组展示（去重）
        ↓
用户选择文件、目标路径、是否移动源文件
        ↓
点击 Move Files 按钮
        ↓
验证选择（至少选择一个文件或勾选移动源文件）
        ↓
创建目标文件夹 (createFolderIfNotExists)
        ↓
遍历移动引用文件 (moveFiles)
        ↓
如勾选则移动源文件
        ↓
保存移动历史 (Storage.addRecord)
        ↓
显示成功提示，2秒后自动关闭
```

### 4.3 撤销移动流程

```
用户触发撤销历史命令
        ↓
打开 UndoMoveModal
        ↓
获取并按时间戳分组历史记录 (groupHistoryByTimestamp)
        ↓
用户选择要撤销的批次
        ↓
点击 Undo All 按钮
        ↓
按时间戳筛选要撤销的记录
        ↓
遍历将文件移回原始位置
        ↓
从历史记录中删除该时间戳的所有记录
        ↓
刷新界面，显示撤销结果
```

***

## 5. API 说明

### 5.1 Obsidian API 使用

| API                                   | 用途       | 来源         |
| :------------------------------------ | :------- | :--------- |
| `app.vault.read()`                    | 读取文件内容   | `obsidian` |
| `app.fileManager.renameFile()`        | 移动/重命名文件 | `obsidian` |
| `app.vault.createFolder()`            | 创建文件夹    | `obsidian` |
| `app.vault.getAbstractFileByPath()`   | 根据路径获取文件 | `obsidian` |
| `app.workspace.getActiveViewOfType()` | 获取当前活动视图 | `obsidian` |
| `new Notice()`                        | 显示通知提示   | `obsidian` |
| `this.addCommand()`                   | 注册命令     | `Plugin`   |
| `this.addRibbonIcon()`                | 注册侧边栏图标  | `Plugin`   |
| `app.metadataCache.resolvedLinks`     | 获取已解析的文件链接 | `obsidian` |
| `app.metadataCache.getFileCache()`    | 获取文件缓存（包含嵌入） | `obsidian` |
| `app.metadataCache.getFirstLinkpathDest()` | 解析链接路径到文件 | `obsidian` |

***

## 6. 开发环境配置

### 6.1 依赖安装

```bash
npm install
```

### 6.2 开发命令

| 命令              | 描述               |
| :-------------- | :--------------- |
| `npm run dev`   | 开发模式，监听文件变化并自动编译 |
| `npm run build` | 生产构建，生成 main.js  |
| `npm run lint`  | 代码检查             |

### 6.3 TypeScript 配置

主要配置项说明：

| 配置项                | 值           | 说明       |
| :----------------- | :---------- | :------- |
| `target`           | ES6         | 编译目标版本   |
| `module`           | ESNext      | 模块系统     |
| `moduleResolution` | node        | 模块解析方式   |
| `strict`           | true        | 启用严格类型检查 |
| `lib`              | ESNext, DOM | 包含的标准库   |

***

## 7. 安装与测试

### 7.1 开发环境安装

1. 克隆项目到本地
2. 执行 `npm install` 安装依赖
3. 执行 `npm run dev` 启动开发模式
4. 在 Obsidian 中开启开发者模式
5. 进入「第三方插件」页面，点击「浏览」选择项目文件夹

### 7.2 生产环境安装

1. 执行 `npm run build` 构建
2. 将构建产物复制到 Obsidian 插件目录
3. 在 Obsidian 设置中启用插件

***

## 8. 代码规范

### 8.1 命名规范

- **类名**: PascalCase（如 `MoveFileModal`）
- **方法名**: camelCase（如 `analyzeReferencedFiles`）
- **变量名**: camelCase（如 `sourceFile`）
- **接口名**: PascalCase（如 `ReferencedFile`）
- **常量**: UPPER_CASE（如 `STORAGE_KEY`）
- **对象属性**: camelCase（如 `Storage.getHistory`）

### 8.2 代码风格

- 使用 TypeScript 严格模式
- 接口和类需要添加 JSDoc 注释
- 方法参数和返回值需要明确类型
- 避免使用 `any` 类型
- 异步方法使用 `async/await`
- 使用早期返回模式简化控制流

***

## 9. 版本历史

| 版本     | 日期         | 说明                             |
| :----- | :--------- | :----------------------------- |
| V1.2.0 | 2026-05-14 | 新增移动源文件功能；支持按时间戳批量撤销；优化代码结构 |
| V1.1.0 | 2026-05-14 | 新增撤销移动功能；改用 MetadataCache API 获取链接 |
| V1.0.0 | 2026-05-13 | 初始版本，支持引用文件分析、按扩展名分类、文件移动 |

***

## 10. 附录

### 10.1 支持的链接格式

| 格式类型 | 示例 | 说明 | 提取结果 |
| :--- | :--- | :--- | :--- |
| Markdown链接 | `[text](path)` | 标准markdown链接 | `path` |
| Wiki链接 | `[[path]]` | 基础wiki链接 | `path` |
| 嵌入Wiki链接 | `![[path]]` | 带!前缀的嵌入链接 | `path` |
| 带锚点Wiki链接 | `[[path#heading]]` | 指向文档内标题的链接 | `path` |
| 带别名Wiki链接 | `[[path\|alias]]` | 自定义显示文本的链接 | `path` |

### 10.2 排除的链接类型

- HTTP/HTTPS 链接
- mailto 链接

### 10.3 错误处理

| 错误场景   | 处理方式           |
| :----- | :------------- |
| 未选择文件且未勾选移动源文件 | 显示错误提示，阻止移动    |
| 目标路径为空 | 显示错误提示，阻止移动    |
| 文件移动失败 | 继续移动其他文件，记录失败数量 |
| 文件不存在（撤销时） | 跳过该文件，继续撤销其他文件 |